import { PageSizeClassExamsDirective } from './page-size-class-exams.directive';

describe('PageSizeClassExamsDirective', () => {
  it('should create an instance', () => {
    const directive = new PageSizeClassExamsDirective();
    expect(directive).toBeTruthy();
  });
});
